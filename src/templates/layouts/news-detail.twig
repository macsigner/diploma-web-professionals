{% extends 'layouts::base.twig' %}

{% block mainHeader %}
    {% if data.image %}
        {% include '@components/image.twig' with {image: data.image} %}
    {% endif %}

    <div class="base-width">
        <h1>{{ title }}</h1>
        <time datetime="{{ data.date.datetime }}">{{ data.date.locale }}</time>

        {{ data.teaser }}
    </div>

{% endblock %}

{% block main %}

    <div class="base-width">

        {% block mainArticle %}
        {% endblock %}

    </div>

{% endblock %}

{% block mainEnd %}

    <a href="/aktuelles.html">Zurück</a>

{% endblock %}
